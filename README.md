# ugc.dev

## Development

### Setup / Install

  * Make sure you have Node.js 12 installed
  * In the `app` directory:
    * `npm install`

### Dev Server

In the `app` diretory:

```
npm run serve
```

## Deployment

### Hosting

In the `app` directory:

```
npm run build && npm run deploy
```
